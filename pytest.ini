[pytest]
testpaths = tests
markers =
    unit: deterministic tests without server, models, or hardware
    integration: tests requiring a running local backend
    runtime: tests requiring local model runtimes and model files
    hardware: tests requiring microphone, speakers, or GPU
    slow: long-running end-to-end tests
addopts = -ra
